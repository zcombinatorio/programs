.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
sdk/dist/
.env
.sentinel
.keys/
.blinker/
.deploy-cache/

# Surfpool local state
.surfpool/

# Runbooks (may contain keys/sensitive config)
runbooks/

# Test configs with keys
devnet-dlmm-config.json
test-admin.json
.claude/test-admin.json

# Yarn/npm (using pnpm)
yarn.lock
yarn-error.log
.yarnrc*
package-lock.json

# Surfpool manifest (contains API keys)
txtx.yml
