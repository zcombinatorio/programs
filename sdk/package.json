{
  "name": "@zcomb/programs-sdk",
  "version": "1.12.1",
  "description": "SDK for ZCombinator Programs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npm run minify",
    "minify": "find dist -name '*.js' -exec terser {} -c -m --output {} \\;",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zcombinatorio/programs.git",
    "directory": "programs/sdk"
  },
  "keywords": [
    "solana",
    "sdk",
    "anchor",
    "vault",
    "amm",
    "futarchy"
  ],
  "files": [
    "dist",
    "README.md"
  ],
  "license": "AGPL-3.0",
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/spl-token": "^0.4.14",
    "@solana/web3.js": "^1.98.0",
    "@sqds/multisig": "^2.1.4"
  },
  "devDependencies": {
    "terser": "^5.46.0",
    "typescript": "^5.0.0"
  }
}
